<!-- You should put style tags into your main.handlebars, or separate css file. I could definitely see having a navbar separate and in your main.handlebars would be much more readable. Also, utilize indents for spacing in your html for better readability-->
<style type="text/css">
    <!--
    html, body, #tbl_wrap { height: 100%; width: 100%; padding: 0; margin: 0; }
    #td_wrap { vertical-align: middle; text-align: center; }
    -->
    </style>
 <table id="tbl_wrap"><tbody><tr><td id="td_wrap">

<h1>cats cats cats</h1>

<ul>
{{#each pages}}
<a href='{{this.name}}'>{{this.label}}</a>
<br />
{{/each}}
</ul>

<ul>
{{#each pages}}
{{this.message}}
<br />
{{/each}}
</ul>

<ul>
{{#each color}}
<a href='{{this.name}}'>{{this.label}}</a>
<br />
{{/each}}
</ul>

<!-- Not quite sure if this is even used? -->
</td></tr></tbody></table>
